name: Hoshi
options:
  bundleIdPrefix: com.hoshi
  deploymentTarget:
    iOS: "18.0"
  xcodeVersion: "16.4"
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    "EXCLUDED_ARCHS[sdk=iphonesimulator*]": x86_64

packages:
  Citadel:
    url: https://github.com/orlandos-nl/Citadel.git
    from: "0.12.0"
  CryptoSwift:
    url: https://github.com/krzyzanowskim/CryptoSwift.git
    from: "1.8.0"

targets:
  Hoshi:
    type: application
    platform: iOS
    sources:
      - path: Hoshi
        excludes:
          - Info.plist
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: "YES"
        INFOPLIST_FILE: Hoshi/Info.plist
        INFOPLIST_KEY_UILaunchScreen_Generation: true
        INFOPLIST_KEY_NSFaceIDUsageDescription: "Hoshi uses Face ID to protect your terminal sessions."
        INFOPLIST_KEY_NSMicrophoneUsageDescription: "Hoshi uses your microphone only while you hold the voice prompt button; audio is transcribed on this device."
        INFOPLIST_KEY_NSSpeechRecognitionUsageDescription: "Hoshi transcribes voice prompts on this device so you can review them before sending them to your terminal."
        INFOPLIST_KEY_CFBundleDisplayName: "Hoshi"
        INFOPLIST_KEY_NSSupportsLiveActivities: true
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight
        "INFOPLIST_KEY_UISupportedInterfaceOrientations~ipad": UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight
        # Preserve the existing release declaration; review export compliance before shipping.
        ITSAppUsesNonExemptEncryption: "NO"
        PRODUCT_BUNDLE_IDENTIFIER: app.gethoshi.ios
        MARKETING_VERSION: "1.1.0"
        CURRENT_PROJECT_VERSION: "4"
        DEVELOPMENT_TEAM: UXWHD5F6F4
        CODE_SIGN_STYLE: Automatic
        TARGETED_DEVICE_FAMILY: "1,2"
    dependencies:
      - target: HoshiLiveActivity
      - package: Citadel
      - package: CryptoSwift
      - framework: Frameworks/GhosttyKit.xcframework
        embed: false

  HoshiLiveActivity:
    type: app-extension
    platform: iOS
    sources:
      - path: HoshiLiveActivity
        excludes:
          - Info.plist
      - path: Hoshi/Models/AgentLiveActivityAttributes.swift
    info:
      path: HoshiLiveActivity/Info.plist
      properties:
        CFBundleDisplayName: Hoshi Live Activity
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        GENERATE_INFOPLIST_FILE: "YES"
        INFOPLIST_FILE: HoshiLiveActivity/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: app.gethoshi.ios.liveactivity
        MARKETING_VERSION: "1.1.0"
        CURRENT_PROJECT_VERSION: "4"
        DEVELOPMENT_TEAM: UXWHD5F6F4
        CODE_SIGN_STYLE: Automatic
        APPLICATION_EXTENSION_API_ONLY: "YES"
        TARGETED_DEVICE_FAMILY: "1,2"

  HoshiTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - HoshiTests
    dependencies:
      - target: Hoshi
    settings:
      base:
        GENERATE_INFOPLIST_FILE: "YES"
        PRODUCT_BUNDLE_IDENTIFIER: com.hoshi.HoshiTests
        TARGETED_DEVICE_FAMILY: "1,2"
